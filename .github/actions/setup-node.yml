name: Setup Node
description: Set up Node environment for CI workflows
runs:
  using: "composite"
  steps:
  - name: Set up pnpm
    uses: pnpm/action-setup@v6.0.8
  
  - name: Set up Node
    uses: actions/setup-node@v6.4.0
    with:
      node-version: "24"
      cache: pnpm
